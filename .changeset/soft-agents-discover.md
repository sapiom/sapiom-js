---
"@sapiom/harness": minor
---

Discover markerless `defineAgent` and legacy `defineOrchestration` projects with bounded syntax-only analysis, conservative reconciliation, and live rail/system-graph updates without executing project code. Markerless agents inside nested Git repositories are discovered when that repository is selected directly.
