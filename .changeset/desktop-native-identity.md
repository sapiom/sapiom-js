---
"@sapiom/opencode": patch
---

Verify the owned native binary in signed macOS desktop bundles using an app-sealed signing receipt while preserving original artifact checks outside packaged Electron.
