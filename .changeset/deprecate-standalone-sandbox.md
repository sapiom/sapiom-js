---
"@sapiom/sandbox": patch
---

Deprecate for new projects. `@sapiom/tools` now ships sandbox support built in (`sapiom.sandboxes.*`), which new projects should use instead. This standalone package remains published and supported for existing production use. Adds a deprecation notice to the README.
