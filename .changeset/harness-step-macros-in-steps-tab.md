---
"@sapiom/harness": patch
---

The step debug macros — Why slow / Debug / Explain / free-form, each injecting that step's context (status, latency, error, input/output, calls, logs) into the terminal for the coding agent — now live in the Steps tab, per step, right next to that step's logs, instead of in the Canvas step inspector. The Canvas step inspector is now purely for reading a step's data. Same inject behavior, styled to match the Steps tab.
