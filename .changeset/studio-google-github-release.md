---
"@sapiom/harness-desktop": patch
---

Bundle the Google and GitHub capability clients and fix local agent checks and runs when dependencies such as Google client libraries call `require()` at runtime.
