---
"@sapiom/harness": patch
---

- Deployment version popover: click the Deployed / Draft chip in the run bar to see the definition id, the latest build and when it deployed, a dashboard link, and a Redeploy action.
- Clearer recovery when a workflow is linked to an agent that isn't on your account: Run and Deploy now explain "Deploy it first to publish it under your account" instead of a bare "definition not found".
