---
"@sapiom/mcp": patch
---

Name email, domains, and memory in the `AUTHORING_INSTRUCTIONS` capability list and note that schedules are a top-level `@sapiom/tools` import — kept byte-identical with the backend's served copy (SAP-1467).
