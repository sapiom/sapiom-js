---
"@sapiom/harness": minor
---

Add `agent_map_implementations` to list exact same-project Studio implementations and `agent_map_bind` to link or unlink Agent Map nodes with revision checks. Expose protected `GET /api/projects/:projectId/agent-map/implementations` and `GET /api/projects/:projectId/agent-map/nodes/:nodeId/implementation` endpoints for binding summaries and exact local navigation targets.
