---
"@sapiom/tools": minor
---

Add browserAutomation.tasks and managed session creation, recovery, and cleanup methods. Existing session methods keep their current API. The new methods require the browser gateway API at /v1/browser.
