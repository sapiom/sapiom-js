---
"@sapiom/opencode": patch
---

Isolate native OpenCode configuration discovery behind an ephemeral home and scrub runtime credentials from tool environments while preserving caller home-directory semantics.
