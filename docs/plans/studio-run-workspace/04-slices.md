# Vertical Slices: Studio Run Workspace

1. **Tracer bullet:** persist the approved mockups, expose an input contract, and wire a raw-JSON Run sheet through both existing launch targets.
2. **Rich inputs:** replace the tracer editor's happy path with recursive schema controls, AJV validation, and per-agent input/target restoration.
3. **Live evidence:** preserve cloud audit fields and stream local attempt start/settle evidence into stable upserts.
4. **Adaptive workspace:** switch selected runs to a chronological timeline with narrow drill-in and Focus split layouts using one evidence inspector.
5. **Artifacts:** lead successful runs with safe rendered artifacts and retain exact Raw/Copy fallbacks.
6. **Integration polish:** enrich coding-agent prompts, add privacy-safe success instrumentation, complete accessibility, and run/update the full regression suite.

All six slices were completed on 2026-08-13. The implementation deliberately
keeps production capability calls absent when the upstream projection has no
call records, while local calls render when the stub trace recorded them.

7. **Hands-on remediation:** make the desktop dev build coherent, sanitize the
   PTY colour environment, replace duplicated run-workspace composition with a
   single adaptive layout, add bounded/foldable artifact primitives, isolate
   Focus at the document layer, and pin all reported behaviors with mocked tests.

Slice 7 was explicitly requested after hands-on review and completed on
2026-08-13. Its acceptance suite uses deterministic mock contracts, runs,
artifacts, window framing, terminal output, and launcher environment values.
