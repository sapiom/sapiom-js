{
  "name": "@sapiom/example-content-repurposing-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sapiom agent template: turn one blog post or transcript into a tweet thread, LinkedIn post, newsletter, quote graphics, and a short clip with an LLM (llm.run), async image + video generation, then package it to file storage and fan it out by email to every recipient on the list.",
  "scripts": {
    "test": "tsx --test index.test.mjs",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "dependencies": {
    "@sapiom/agent": "^0.12.0",
    "@sapiom/tools": "^0.31.0",
    "zod": "^3.25.76 || ^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "tsx": "^4.23.0",
    "typescript": "^5.4.2"
  }
}
