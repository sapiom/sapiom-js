{
  "name": "@sapiom/example-research-to-microsite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sapiom agent template: research a topic across the web, self-critique and revise a cited report with an LLM, illustrate it, then have a coding agent build and deploy a shareable live micro-site — with an optional custom domain.",
  "scripts": {
    "test": "tsx --test index.test.mjs",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "dependencies": {
    "@sapiom/agent": "^0.12.0",
    "@sapiom/tools": "^0.31.0",
    "zod": "^3.25.76 || ^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "tsx": "^4.23.0",
    "typescript": "^5.4.2"
  }
}
