{
  "$schema": "../template.schema.json",
  "manifestVersion": 1,
  "author": {
    "name": "Sapiom",
    "url": "https://sapiom.ai/"
  },
  "whatItDoes": "Research a topic across the web, grade and revise the write-up against an editorial rubric, illustrate it, then build and deploy a live report site. Reads the top sources in full, self-critiques the draft before it publishes, and can map the result onto a subdomain you own.",
  "longDescription": "Give it a topic. It plans a couple of complementary search queries, gathers and dedupes candidate sources, reads the survivors in full, and has an LLM write a structured, cited report with a title, a summary, and sections that reference where each claim came from. A second model call then grades the report against a fixed editorial bar (every section cites a source, no section restates another, the tone fits the audience) and revises it if it falls short, bounded to a couple of attempts. It generates one or two illustrations and hands the package to a coding agent that builds and deploys a self-contained static site, returning a URL.\n\nPass `maxDraftAttempts: 1` to skip the revise loop, or `illustrationCount: 0` to skip images. The site build is a real coding-agent run: `build` launches it and pauses until it's done, waking up at `publish` once the site is ready. `publish` deploys the site to a preview URL, and pointing a domain you own at it with `mapDomain` maps the site onto that domain instead. Set `dryRun: true` to stop once the report clears self-critique and get it back without illustrating, building, or deploying anything.",
  "useCases": [
    "Live, linkable report site",
    "Self-graded before it ships",
    "Illustrated, cited sections"
  ],
  "notes": "Click **Use this template**. Sapiom builds and deploys it for you; open it on the **Agents** page and run it.\n\nGive it a `topic` (what to research) and, optionally, an `audience` to tune the tone. `reviewThreshold` (default 0.7) and `maxDraftAttempts` (default 2) control the self-critique loop. `illustrationCount` (default 2, max 3) controls how many sections get a generated image; set it to 0 to skip illustration entirely. To publish under your own domain, register or own the domain in Sapiom first, then pass `customDomain` (a domain you own) and an optional `subdomain` (defaults to `www`) to add a CNAME to the live site. Leave the domain off and you still get a shareable preview URL. Pass `dryRun: true` to get the report back after self-critique, without illustrating, building, or deploying anything.\n\nPrefer to work from the code? Run it locally with `run_local` and `{ \"topic\": \"...\", \"dryRun\": true }` to trace plan, gather, synthesize, and critique, and read the report (capabilities stubbed, nothing built), or edit and deploy it with the Sapiom MCP.",
  "defaultInput": {
    "topic": "how durable agent runtimes handle retries"
  },
  "examples": [
    {
      "title": "Preview the graded report before publishing",
      "input": {
        "topic": "the state of on-device AI in 2026",
        "audience": "developers",
        "dryRun": true
      },
      "output": {
        "published": false,
        "dryRun": true,
        "reason": "dry-run",
        "topic": "the state of on-device AI in 2026",
        "reviewScore": 0.88,
        "reviewPassed": true,
        "reviewIterations": 1,
        "report": {
          "title": "On-Device AI in 2026",
          "tagline": "Smaller models, bigger reach — inference moves to the edge.",
          "summary": "On-device inference has crossed from demo to default for a growing class of apps...",
          "sections": [
            {
              "heading": "What changed",
              "body": "Quantized small models now run acceptably on consumer hardware [1], and toolchains ship first-class on-device runtimes [2]."
            }
          ],
          "sources": [
            {
              "title": "Small models on the edge",
              "url": "https://example.com/edge"
            },
            {
              "title": "On-device runtimes",
              "url": "https://example.com/runtimes"
            }
          ]
        }
      }
    },
    {
      "title": "Publish a live, illustrated site under a custom domain",
      "input": {
        "topic": "the state of on-device AI in 2026",
        "audience": "developers",
        "customDomain": "your-domain.dev",
        "subdomain": "report"
      },
      "output": {
        "published": true,
        "topic": "the state of on-device AI in 2026",
        "title": "On-Device AI in 2026",
        "tagline": "Smaller models, bigger reach — inference moves to the edge.",
        "liveUrl": "https://research-to-microsite-abc123.preview.sapiom.ai",
        "customUrl": "https://report.your-domain.dev",
        "sandboxName": "research-to-microsite-abc123",
        "reviewScore": 0.88,
        "reviewPassed": true,
        "reviewIterations": 1,
        "illustrationCount": 2,
        "sources": [
          {
            "title": "Small models on the edge",
            "url": "https://example.com/edge"
          },
          {
            "title": "On-device runtimes",
            "url": "https://example.com/runtimes"
          }
        ]
      }
    }
  ],
  "zeroSetup": {
    "terminalState": "completed",
    "expect": [
      {
        "path": "liveUrl",
        "assert": "nonEmptyString"
      },
      {
        "path": "sources",
        "assert": "nonEmptyArray"
      },
      {
        "path": "title",
        "assert": "nonEmptyString"
      }
    ],
    "narrative": "Researches, self-critiques, illustrates, and deploys a live, cited micro-site about a default topic."
  }
}
