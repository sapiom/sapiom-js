{
  "name": "@sapiom/example-scheduled-research-brief",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sapiom agent template: on a cron cadence, search + scrape a topic, curate the findings into a sourced brief with an LLM (llm.run), and deliver it by email.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "dependencies": {
    "@sapiom/agent": "^0.12.0",
    "@sapiom/tools": "^0.31.0",
    "zod": "^3.25.76 || ^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.2"
  }
}
