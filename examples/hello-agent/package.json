{
  "name": "@sapiom/example-hello-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sapiom agent template: the minimal single-step agent — a smoke test for the build → deploy → run path.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "dependencies": {
    "@sapiom/agent": "^0.5.0",
    "@sapiom/tools": "^0.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.2"
  }
}
