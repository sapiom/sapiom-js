{
  "$schema": "../template.schema.json",
  "manifestVersion": 1,
  "author": {
    "name": "Sapiom",
    "url": "https://sapiom.ai/"
  },
  "whatItDoes": "Route a contract, budget, or policy change through an ordered chain of approvers who each sign off in turn. Nudges a slow approver, escalates when a gate goes silent, and unwinds cleanly if anyone rejects. Every decision lands in your own Postgres as an audit trail.",
  "longDescription": "Route a subject like a contract, budget, or policy change through an ordered chain of approvers who each sign off in turn. Each gate is a durable `pauseUntilSignal` that suspends the run until that approver fires `approval.decision`: an approve advances to the next gate, a reject unwinds and notifies everyone who already signed. Silent gates get reminders up to `maxReminders`, then escalate. Every transition is appended to a Postgres audit trail via the `database` capability, degrading to `ctx.shared` and logs if the store is unreachable.",
  "useCases": [
    "Ordered sign-off chain",
    "Reminders and escalation",
    "Audit trail in your Postgres"
  ],
  "notes": "`run_local` stubs the capabilities and auto-resumes each pause with no payload, so the default offline trace walks present → reminder(s) → escalate to a terminal for free, and the stubbed database means no live Postgres is touched. Set a small `maxReminders` (default 2) so the offline trace terminates quickly.\n\nA real `deploy` + `run` pauses for real at each gate. To drive it in dev, fire the signals via the MCP `signal_workflow` / `workflow_signal` tool with `correlationId` = the paused run's `executionId`. Approve a gate: `{ \"signal\": \"approval.decision\", \"correlationId\": \"<executionId>\", \"payload\": { \"decision\": \"approve\" } }` — repeat once per approver to reach `finalize`. Send `{ \"decision\": \"reject\" }` to walk the compensation path, or `{ \"decision\": \"remind\" }` / `{ \"decision\": \"timeout\" }` to exercise the reminder / escalation branches.\n\nThe audit trail persists to a Sapiom Postgres by default (handle `approval-chain-ledger`); point `ledgerHandle` (or `config.LEDGER_HANDLE`) at your own database to use that instead, or clear it to keep the trail in the run's output only. Run deployed with `dryRun: false` and verify the rows land in the `approval_chain_ledger` table. See `README.md` and `AGENTS.md`.",
  "resources": [
    {
      "kind": "postgres",
      "handle": "approval-chain-ledger",
      "duration": "7d",
      "ephemeral": false,
      "reuse": { "key": "ledgerHandle" }
    }
  ],
  "settings": [
    {
      "path": "ledgerHandle",
      "label": "Audit ledger database handle",
      "description": "The Sapiom Postgres each sign-off transition is appended to. Leave the default to use the database Sapiom provisions for this template, or point it at a handle you already own. Cleared, the audit trail stays in the run's output only.",
      "type": "string",
      "default": "approval-chain-ledger",
      "example": "approval-chain-ledger"
    },
    {
      "path": "escalateTo",
      "label": "Escalate to",
      "description": "Where a stalled or unapprovable chain is handed off. Leave it empty and the escalation stays in the run's output.",
      "type": "email",
      "default": "",
      "example": "you@yourcompany.com"
    }
  ],
  "defaultInput": {
    "subject": "Sample: renew the annual analytics contract ($18,000)"
  },
  "examples": [
    {
      "title": "Two-party contract sign-off, both approve",
      "input": {
        "subject": "Master Services Agreement v3",
        "approvers": [
          {
            "id": "legal",
            "name": "Legal",
            "email": "legal@example.com"
          },
          {
            "id": "finance",
            "name": "Finance",
            "email": "finance@example.com"
          }
        ],
        "escalateTo": "ops@example.com",
        "maxReminders": 2
      },
      "output": {
        "committed": true,
        "dryRun": false,
        "outcome": "completed",
        "subject": "Master Services Agreement v3",
        "notified": 2
      }
    },
    {
      "title": "Finance rejects after legal approved — compensate",
      "input": {
        "subject": "Q3 marketing budget increase",
        "approvers": [
          {
            "id": "legal",
            "name": "Legal",
            "email": "legal@example.com"
          },
          {
            "id": "finance",
            "name": "Finance",
            "email": "finance@example.com"
          }
        ],
        "escalateTo": "ops@example.com"
      },
      "output": {
        "committed": false,
        "outcome": "rejected",
        "subject": "Q3 marketing budget increase",
        "rejectedBy": "finance",
        "compensated": 1
      }
    }
  ],
  "zeroSetup": {
    "terminalState": "completed_partial",
    "expect": [
      { "path": "committed", "assert": "equals", "value": false },
      { "path": "trail", "assert": "nonEmptyArray" },
      { "path": "unmet", "assert": "nonEmptyArray" }
    ],
    "narrative": "Runs the saga with no approvers supplied, so nothing is committed and it escalates."
  }
}
