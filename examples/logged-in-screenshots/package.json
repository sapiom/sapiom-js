{
  "name": "@sapiom/example-logged-in-screenshots",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sapiom agent template: crawl a bounded set of a site's pages, render-check and screenshot each one, audit content with a model, and verify link integrity (broken links, Terms/Privacy present and resolving).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "dependencies": {
    "@sapiom/agent": "^0.9.0",
    "@sapiom/tools": "^0.25.0",
    "zod": "^3.25.76 || ^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.2"
  }
}
