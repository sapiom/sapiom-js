{
  "name": "@sapiom/example-eval-gate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sapiom agent template: a self-editing writer — draft against your brief with llm.run, judge the draft against your rubric with a second chained llm.run call, and revise in a bounded loop until it clears the bar or the attempt cap is hit.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "dependencies": {
    "@sapiom/agent": "^0.12.0",
    "@sapiom/tools": "^0.31.0",
    "zod": "^3.25.76 || ^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.2"
  }
}
