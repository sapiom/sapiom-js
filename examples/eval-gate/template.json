{
  "$schema": "../template.schema.json",
  "manifestVersion": 1,
  "author": {
    "name": "Sapiom",
    "url": "https://sapiom.ai/"
  },
  "whatItDoes": "Score an output against your own rubric with an LLM judge, then branch on whether it clears your threshold. You supply the case, the output, and the bar; it returns a decision you can publish on, revise on, or ask another agent to grade as a child run.",
  "longDescription": "An LLM-judge quality gate. You bring the rubric; it handles the scoring and the branch. Pass it the case, the output to grade, your criteria, and a pass bar, and it tells you whether the output is good enough — and why.\n\nThe judge step renders a prompt from your rubric and makes one metered LLM call through `ctx.sapiom.models.run`, then reads a 0-to-1 score out of the reply. It prefers a JSON `{score, rationale}`, falls back to a bare number, clamps to `[0,1]`, and handles a judge that answered on a 0–100 scale. The gate step is pure logic: score at or above the threshold goes to publish, below goes to revise. The two terminal steps hand back `{ decision, score, threshold, rationale }`, and what those decisions mean is up to you.\n\nThe judge prompt and score parser are the only new code; the rubric, the threshold, and the judge model are all yours to change. Because it's a deployable agent, a parent agent can also launch it as a child run to grade its own output and branch on the result.\n\nYou pay for the one judge model call per run. There's nothing else to meter.",
  "useCases": [
    "Your rubric, your threshold",
    "Publish or revise branch",
    "Runs as a child agent"
  ],
  "notes": "Click **Use this template** — Sapiom builds and deploys it for you; open it on the **Agents** page and run it. Your $5 signup credit covers first runs.\n\nGive it a run input: the `input` (the case), the `output` to grade, your `rubric` string, and optionally a `threshold` (default 0.7) and a judge `model`. The rubric is where all your opinion about quality lives — raise the threshold to be stricter, lower it to let more through.\n\nPrefer to work from the code? Run it locally with `run_local` to trace both branches for free — but the default stub reply carries no number and the parser throws on that by design, so supply a stub score (a high one routes to publish, a low one to revise). Then edit the rubric and deploy it with the Sapiom MCP.",
  "defaultInput": {
    "input": "Write a one-line product tagline for a privacy-first email app.",
    "output": "Inbox peace, finally — email that never reads your mail.",
    "rubric": "Concise (<= 12 words), mentions privacy, no jargon, reads naturally.",
    "threshold": 0.7
  },
  "examples": [
    {
      "title": "High score → publish",
      "input": {
        "input": "Write a one-line product tagline for a privacy-first email app.",
        "output": "Inbox peace, finally — email that never reads your mail.",
        "rubric": "Concise (<= 12 words), mentions privacy, no jargon, reads naturally.",
        "threshold": 0.7
      },
      "output": {
        "decision": "publish",
        "score": 0.9,
        "threshold": 0.7,
        "rationale": "Concise, mentions privacy, reads naturally."
      }
    },
    {
      "title": "Low score → revise",
      "input": {
        "input": "Write a one-line product tagline for a privacy-first email app.",
        "output": "The best email application in the entire world for everyone everywhere.",
        "rubric": "Concise (<= 12 words), mentions privacy, no jargon, reads naturally.",
        "threshold": 0.7
      },
      "output": {
        "decision": "revise",
        "score": 0.3,
        "threshold": 0.7,
        "rationale": "Does not mention privacy and reads as generic hype."
      }
    }
  ],
  "zeroSetup": {
    "terminalState": "completed",
    "expect": [
      { "path": "decision", "assert": "nonEmptyString" },
      { "path": "rationale", "assert": "nonEmptyString" }
    ],
    "narrative": "Grades the built-in sample against a rubric and returns a decision with rationale."
  }
}
