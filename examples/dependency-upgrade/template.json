{
  "$schema": "../template.schema.json",
  "manifestVersion": 1,
  "author": {
    "name": "Sapiom",
    "url": "https://sapiom.ai/"
  },
  "whatItDoes": "Bump a repo's dependencies on a schedule without a human driving each one. A coding agent clones the repo into a sandbox, upgrades, and runs your real test suite; it pushes only when the build is green and holds majors for review.",
  "longDescription": "You give it a repo and a schedule. On each tick a coding agent clones the repo into a fresh sandbox and bumps its dependencies, then the run re-attaches that sandbox and runs your real test suite. A red build is dropped; a green build gets a model risk assessment, and only a green build within `maxAutoRisk` is pushed, while anything riskier is held for a human. The push sits behind a `dryRun` guard, and every outcome writes a triage report to file storage.",
  "useCases": [
    "Green-build-only pushes",
    "Cron dependency bumps",
    "Majors held for review"
  ],
  "notes": "Click **Use this template** — Sapiom builds and deploys it for you; open it on the **Agents** page and run it. Your $5 signup credit covers first runs.\n\nGive it a `repoSlug` — an in-network repo the coding agent clones and upgrades. There is deliberately no default: a repository has to exist, and a run without one stops immediately and says so rather than failing partway through. Set `testCommand` (default `npm test`) and `installCommand` (default `npm install`) to match the project, and `maxAutoRisk` (default `medium`) to control what pushes on its own versus what waits for you. To run it on a cadence, attach the `schedule` as a cron trigger on the deployed agent. Pass `dryRun: true` to trace the whole graph without pushing or uploading anything.\n\nPrefer to work from the code? Run it locally with `run_local` (pass your own repo slug plus `\"dryRun\": true`) to trace plan → bump → verify → assess → publish for free — capabilities are stubbed and the push is skipped — or edit and deploy it with the Sapiom MCP.",
  "settings": [
    {
      "path": "repoSlug",
      "label": "Repository to upgrade",
      "description": "An in-network repo the coding agent clones and upgrades. Without one the run stops immediately and says nothing was cloned.",
      "type": "string",
      "default": "",
      "example": "your-repo-slug"
    }
  ],
  "defaultInput": {
    "maxAutoRisk": "medium"
  },
  "examples": [
    {
      "title": "No repository configured — nothing cloned",
      "input": {},
      "output": {
        "decision": "reject",
        "pushed": false,
        "reason": "no-repo",
        "unmet": ["repoSlug"],
        "detail": "No `repoSlug` was given, so there was no repository to upgrade and nothing was cloned, tested, or pushed. Create an in-network repo (`git_create_repo`) or point `repoSlug` at one you already have, then re-run."
      }
    },
    {
      "title": "A dry-run triage of your repo's upgrade",
      "input": {
        "testCommand": "npm test",
        "maxAutoRisk": "medium",
        "dryRun": true
      },
      "output": {
        "decision": "publish",
        "pushed": false,
        "dryRun": true,
        "risk": "low",
        "reportFileId": null,
        "summary": "Patch and minor bumps across dev dependencies; no major versions, narrow blast radius."
      }
    }
  ],
  "zeroSetup": {
    "terminalState": "completed_partial",
    "expect": [
      { "path": "pushed", "assert": "equals", "value": false },
      { "path": "reportFileId", "assert": "nonEmptyString" },
      { "path": "unmet", "assert": "nonEmptyArray" }
    ],
    "narrative": "Runs a coding agent to triage upgrades and writes a report; pushes nothing (no repo)."
  }
}
