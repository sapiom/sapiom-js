{
  "$schema": "../template.schema.json",
  "manifestVersion": 1,
  "author": {
    "name": "Sapiom",
    "url": "https://sapiom.ai/"
  },
  "whatItDoes": "Post a message to Slack using your own bot token. The starter for calling any outside service with your own credential — fork it, repoint the endpoint and the credential, and it notifies Discord, PagerDuty, or your own webhook instead.",
  "longDescription": "Post a message to Slack using your own bot token, which Sapiom hands to the run as an environment variable. Slack is the hook, but the lesson is transferable: store a secret, read it at runtime, call an API, and swap Slack for anything by changing the `fetch` URL and credential key. `validate` settles the config; `post` reads the token and calls Slack directly, never writing it into code. `run_local` runs in dryRun mode, as does a missing credential, returning `posted: false` with `skipped`.",
  "useCases": [
    "Slack message from a run",
    "Bring-your-own credential",
    "Repoint to any webhook"
  ],
  "notes": "Click **Use this template** — Sapiom builds and deploys it for you; open it on the **Agents** page and run it. Your $5 signup credit covers first runs.\n\nThis one needs a secret before it can post for real. It runs with no setup: with no credential set it composes the message, reports `posted: false, skipped: \"no-credential\"`, and says what it would have sent. To post for real, supply `SLACK_BOT_TOKEN` (a bot token with `chat:write`) and a `channel`, or `SLACK_WEBHOOK_URL` for webhook mode — Sapiom asks for these when you use the template. Until you set it, runs trace the full graph and return `posted: false, skipped: \"no-credential\"` instead of failing. For bot mode, also pass a `channel` (like `#general`); webhook mode carries the channel in the URL. To point this at another service, change the `fetch` URL and the credential key in `index.ts` and store that API's key under the matching ref.\n\nPrefer to work from the code? Run it locally with `run_local` to trace the whole flow for free (it skips the network), or edit and deploy it with the Sapiom MCP.",
  "requiredSecrets": [
    {
      "key": "SLACK_BOT_TOKEN",
      "label": "Slack bot token",
      "provider": "slack",
      "credentialKind": "bearer_token",
      "description": "A Slack bot token (`xoxb-…`) with `chat:write`. Without it the run composes the message and posts nothing.",
      "docsUrl": "https://api.slack.com/authentication/basics",
      "optional": true
    },
    {
      "key": "SLACK_WEBHOOK_URL",
      "label": "Slack incoming-webhook URL",
      "provider": "slack",
      "credentialKind": "api_key",
      "description": "Only for `via: \"webhook\"`. The channel is baked into the URL.",
      "docsUrl": "https://api.slack.com/messaging/webhooks",
      "optional": true
    }
  ],
  "settings": [
    {
      "path": "channel",
      "label": "Slack channel",
      "description": "Channel name or id for bot mode. Ignored by webhook mode, where the URL carries it.",
      "type": "string",
      "default": "",
      "example": "#general"
    }
  ],
  "defaultInput": {
    "message": "Hello from Sapiom — this message was composed by the slack-notifier template."
  },
  "examples": [
    {
      "title": "No credential set — composed, not posted",
      "input": {},
      "output": {
        "posted": false,
        "skipped": "no-credential",
        "via": "bot",
        "channel": null,
        "ts": null,
        "unmet": ["SLACK_BOT_TOKEN"],
        "note": "No `SLACK_BOT_TOKEN` is set, so nothing was posted to Slack. The message above is what would have been sent — add the credential and re-run to post it."
      }
    },
    {
      "title": "With a bot token — really posted",
      "input": {
        "message": "Deploy finished.",
        "channel": "#general"
      },
      "output": {
        "posted": true,
        "skipped": null,
        "via": "bot",
        "channel": "C0123456789",
        "ts": "1717171717.000100"
      }
    }
  ],
  "zeroSetup": {
    "terminalState": "completed_partial",
    "expect": [
      { "path": "posted", "assert": "equals", "value": false },
      { "path": "skipped", "assert": "equals", "value": "no-credential" },
      { "path": "unmet", "assert": "nonEmptyArray" }
    ],
    "narrative": "Composes the message and prints it; posts nothing to Slack (no bot token)."
  }
}
