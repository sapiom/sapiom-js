/**
 * Browser terminal UI for the harness (react + xterm), served by the local
 * harness server. Built separately from the Node entrypoints.
 *
 * Placeholder module — intentionally empty for now.
 */
export {};
