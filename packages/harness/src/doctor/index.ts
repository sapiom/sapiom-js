/**
 * Environment diagnostics ("doctor") for the harness: checks like "can
 * node-pty load?" report typed results with remediation hints (see
 * `PtyUnavailableError` in the runtime module for the error side).
 *
 * Placeholder module — intentionally empty for now.
 */
export {};
