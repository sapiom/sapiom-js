/**
 * Local harness server (express + ws): REST session management and a
 * WebSocket bridge that streams raw pty bytes to the terminal UI.
 *
 * Placeholder module — intentionally empty for now.
 */
export {};
